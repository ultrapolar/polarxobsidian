pdf2image>=1.16.0
opencv-python>=4.8.0
pytesseract>=0.3.10
numpy>=1.24.0
openpyxl>=3.1.0
Pillow>=10.0.0
rapidfuzz>=3.0.0
pyspellchecker>=0.7.0
EbookLib>=0.18
beautifulsoup4>=4.12.0
pypdf>=4.0.0

# Audiobook reference (optional — only needed to transcribe MP3/M4B audio)
faster-whisper>=1.0.0
# For NVIDIA GPU transcription on Windows, also install the CUDA runtime libs:
#   pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
# (CPU-only works without these, just slower.)
language_tool_python>=2.8   # optional - LanguageTool layer (needs Java)
